# assets

`senate-logo.png` — the NYMC SOM Student Senate shield, shown in the header.

Not committed yet. Drop the PNG in here with exactly that filename and the
header picks it up automatically; until then the header falls back to the
plain "N" mark, so nothing looks broken in the meantime.

A transparent background is ideal — the source in Drive sits on a white
square, which will show as a white block against the cream header if it
isn't removed first.
